# deploy.yml

---
- name: Deploy React App on GCP VM
  hosts: 34.93.111.35
  gather_facts: false
  become: yes
  tasks:
    - name: Print message
      ansible.builtin.debug:
      msg: Hello world
    
    - name: Copy React app files
      copy:
        src: '/workspace/build'
        dest: '/var/www/html'
